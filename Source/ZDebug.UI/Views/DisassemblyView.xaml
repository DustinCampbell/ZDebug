<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ZDebug.UI.ViewModel"
             xmlns:c="clr-namespace:ZDebug.UI.Controls"
             Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
             FontFamily="Consolas"
             FontSize="14"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Images.xaml"/>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <ItemsControl ItemsSource="{Binding Lines}"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.Template>
                <ControlTemplate>
                    <Border BorderThickness="{TemplateBinding Border.BorderThickness}"
                            Padding="{TemplateBinding Control.Padding}"
                            BorderBrush="{TemplateBinding Border.BorderBrush}"
                            Background="{TemplateBinding Panel.Background}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer Padding="8,2,2,2"
                                      Focusable="False">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ItemsControl.Template>

            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type vm:DisassemblyBlankLineViewModel}">
                    <TextBlock Text=" "/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:DisassemblyRoutineHeaderLineViewModel}">
                    <TextBlock FontWeight="Bold" Text="{Binding Address, StringFormat=\{0:x4\}, Mode=OneWay}"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:DisassemblyInstructionLineViewModel}">
                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{DynamicResource addressWidth}"/>
                            <ColumnDefinition Width="{DynamicResource opcodeWidth}"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition MinHeight="{DynamicResource rowHeight}"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Column="0">
                            <Run Text="  "/><Run 
                                 Foreground="#494949" Text="{Binding Address, StringFormat=\{0:x4\}:, Mode=OneWay}"/>
                        </TextBlock>

                        <TextBlock
                            Grid.Column="1"
                            Text="{Binding OpcodeName, Mode=OneWay}"
                            Foreground="{StaticResource OpcodeColor}"/>

                        <c:InstructionTextDisplayElement
                            Grid.Column="2"
                            Instruction="{Binding Instruction, Mode=OneWay}"
                            SeparatorBrush="{StaticResource SeparatorColor}"
                            ZTextBrush="{StaticResource ZTextColor}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.Resources>

        </ItemsControl>
    </Grid>
</UserControl>
