<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ZDebug.UI.ViewModel"
             Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <Grid>
        <ItemsControl FontFamily="Consolas"
                      FontSize="14"
                      ItemsSource="{Binding Lines}"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.Template>
                <ControlTemplate>
                    <Border BorderThickness="{TemplateBinding Border.BorderThickness}"
                            Padding="{TemplateBinding Control.Padding}"
                            BorderBrush="{TemplateBinding Border.BorderBrush}"
                            Background="{TemplateBinding Panel.Background}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer Padding="8,2,2,2"
                                      Focusable="False">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ItemsControl.Template>

            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type vm:DisassemblyBlankLineViewModel}">
                    <TextBlock Text=" "/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:DisassemblyRoutineHeaderLineViewModel}">
                    <TextBlock FontWeight="Bold" FontSize="15" Text="{Binding Address, StringFormat=\{0:x4\}, Mode=OneWay}"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:DisassemblyInstructionLineViewModel}">
                    <TextBlock>
                        <Run Text="  "/><Run 
                             Foreground="DarkGray" Text="{Binding Address, StringFormat=\{0:x4\}, Mode=OneWay}"/><Run 
                             Foreground="DarkGray" Text=":"/>
                        <Run Text="{Binding OpcodeName, Mode=OneWay}"/>
                    </TextBlock>
                </DataTemplate>
            </ItemsControl.Resources>

        </ItemsControl>
    </Grid>
</UserControl>
